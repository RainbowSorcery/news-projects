
spring:
  application:
    name: gateway-service
  cloud:
    nacos:
      config:
        server-addr: 10.0.4.3:8848
        file-extension: YAML
        shared-configs:
          - data-id: nacos-register.YAML
            refresh: true
          - data-id: redis-config.YAML
            refresh: true
